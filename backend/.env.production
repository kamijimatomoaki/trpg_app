# Production Environment Variables for Cloud Run
# Copy this file to .env and fill in the actual values

# Google Cloud Project Configuration
PROJECT_ID=your-google-cloud-project-id
LOCATION=us-central1

# Firebase Admin SDK
# In Cloud Run, this should point to the mounted secret
FIREBASE_ADMIN_KEY_PATH=/secrets/firebase-key.json

# CORS Origins (Frontend URL)
ALLOWED_ORIGINS=https://your-frontend-domain.web.app,https://your-custom-domain.com

# Optional: Additional environment variables
# PORT=8080  # Cloud Run will set this automatically
# GOOGLE_APPLICATION_CREDENTIALS=/secrets/firebase-key.json